spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    root: INFO
    com.eureka_server: DEBUG
eureka:
  server:
    wait-time-in-ms-when-sync-empty: 0
